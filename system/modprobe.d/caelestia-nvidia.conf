# Explicit Wayland-safe defaults. Recent nvidia-utils enables modeset/fbdev
# already; keeping them here also supports older compatible releases.
options nvidia-drm modeset=1 fbdev=1
options nvidia NVreg_PreserveVideoMemoryAllocations=1 NVreg_TemporaryFilePath=/var/tmp
